#include <stdio.h>
/**
 * main - A Program that prints all single digit numbers
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
